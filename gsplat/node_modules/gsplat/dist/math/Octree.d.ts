import { Vector3 } from "./Vector3";
import { Box3 } from "./Box3";
declare class OctreeNode {
    bounds: Box3;
    points: Vector3[];
    depth: number;
    children: OctreeNode[] | null;
    pointIndices: number[];
    constructor(bounds: Box3, points: Vector3[], pointIndices: number[], depth?: number);
    private split;
    queryRange(range: Box3): number[];
}
declare class Octree {
    root: OctreeNode;
    constructor(bounds: Box3, points: Vector3[]);
    queryRange(range: Box3): number[];
}
export { Octree };
